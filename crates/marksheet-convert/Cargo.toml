[package]
name = "marksheet-convert"
description = "Bounded, deterministic conversions between Marksheet IR, CSV, and OOXML workbooks"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
marksheet-calc = { path = "../marksheet-calc" }
marksheet-model = { path = "../marksheet-model" }
quick-xml = "0.41.0"
serde.workspace = true
serde_json.workspace = true
time.workspace = true
zip = { version = "=7.2.0", default-features = false, features = ["deflate-flate2-zlib-rs"] }

[dev-dependencies]
marksheet-syntax = { path = "../marksheet-syntax" }

[lints]
workspace = true
